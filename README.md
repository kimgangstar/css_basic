# CSS 
html이 구조를 만들고 (뼈대) css가 (디자인) js는 (동적요소)
* html 작성 : `<body>``<tag>``</body>`
* css 작성 : `<head>``<stlye>``tag` `{디자인의 속성:값; 속성:값;}``</head>` <!-- 세미콜론 중요! -->
* 
## css 선택자
* 태그 선택자 : `태그 {속성:값;}`
* 아이디 선택자 : `#아이디명 {속성:값;}`
* 클래스 선택자 : `.클래스명 {속성:값;}`
* 부모-자식 태그 선택자 `#부모 > 자식 {속성:값;}`
* 부모-자손 선택자 `부모 자손 {속성:값;}`
* 형제 선택자 `기준형제+선택형제 {속성:값;}`
* 다중 형제 선택자 `기준형제~선택형제들 {속성: 값;}`
* 모든 선택자 : `* {속성:값}`

## css선택자 우선순위
* `<style>` > `#` > `.` > `<tag>`
1. `<tag style="">` 우선순위 높음
2. `#id {}`
3. `.class {}`
4. `tag {}` 우선순위 낮음

## css 본인 적용과 상속 적용 차이
* 자식이 2개 이상일 때 부모의 속성을 적용하는 것으로 자식에게 공통 값을 적용할 수 있다.
    * `ul-li*5` 관계 시 `ul {color:red}` 부모 ul의 color를 li 자식에게 상속시킨다.
    * 꾸미려는 css 속성의 글자에 관련된 속성 (글꼴,글자크기,글자색상 등) 본인에게 직접 적용하는 것을 권장함.(우선순위가 꼬여서)

## 글꼴 사용 시 주의사항
* 모든 컴퓨터 기본글꼴 (고딕 , 궁서 , 바탕 등)이 아니라면 반드시 웹글꼴을 사용하여 웹접근성을 높여줘야함.
* 웹글꼴 연결 순서는 작성한 css보다 반드시 위에 작성하기!
* 글꼴명이 한글이거나 공백이 포함된 경우 따옴표("ex") 붙여서 작성하기! 

## 글자에 간격 넣는법
/* 부모#weather 안 모든(*) 자식(>) */
#weather > * {padding-bottom:20px;}

# 순서
0. 디자인 (피그마 완성)
1. HTML 작성
2. 외부 CSS연결
3. 초기화 CSS연결
4. 글꼴 설정
5. 선택자 모두 작성
    선택자 {}
6. 피그마 디자인과 비교하면서 CSS속성:값; 작성하기


# 인라인을 블록처럼 인식하기 display : block;
(ex) #news > a {display: block;}

## 자주 사용하는 css 속성 모음
* `width:0px;` : 가로크기 (vw , % 상대크기단위가능)
* `height:0px;` : 세로크기 (vh , % 상대크기단위가능)
* `padding:0px;` : 안쪽여백 (피그마 autolayout 패딩)
* `display: ;` : 요소의 형태유형을 변경하는 속성
    * `block` , `inline` , `inline-block`
    * 인라인이 될 수 있는 요소들 : 블록들
        * `h1 {display:inline}`
        * 내용만큼만 크기 인식
        * 옆으로 나열
    * 블록이 될 수 있는 요소들 : 인라인들 
        * `a{display:block}`
        * 크기와 여백값을 인식
        * 옆으로 정렬이 아닌 새로운 행으로 나열
    * inline-block 값은 모두 적용 가능
        * `h1 {display:inline-block}`
        * `a{display:inline-block}`
        * 크기와 여백값을 인식
        * 옆으로 나열 , 넘치면 자동 줄바꿈됨.
